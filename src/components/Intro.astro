---
---

<section class="container">
  <div class="grid items-baseline gap-4 md:gap-8 lg:grid-cols-2">
    <p class="lead">
      As a seasoned designer with roots in the agency world and over two decades
      of freelancing experience, I’ve witnessed firsthand the digital
      landscape's and industry's transformation. This extensive journey has
      equipped me with the expertise to navigate and adapt to the ever-evolving
      design challenges.
    </p>
    <div class="text">
      <p>
        Specializing in solving complex issues practically, my work ensures that
        end users achieve their goals while aligning seamlessly with company
        objectives. As a UX and visual designer, I merge these crucial elements
        to create cohesive, effective designs. Thriving on asking insightful
        questions, my approach addresses intricate concepts and delivers
        efficient, accurate results without losing the fun.
      </p>
      <p>
        As the co-owner of All This, a digital design studio, I bring a unique
        perspective to every project. For those seeking top-tier design
        solutions, visit allthis.digital.
      </p>
    </div>
  </div>
</section>

<style lang="scss">
  .lead {
    font-family: var(--font-family-default);
    font-size: var(--step-1);
    font-weight: 500;
    letter-spacing: 0.00125em;
    line-height: calc(var(--step-1) * 1.33);
  }

  p {
    font-family: var(--font-family-default);
    font-size: calc(var(--step-1) * 0.675);
    font-weight: 400;
    line-height: calc(var(--step-1) * 0.675 * 1.55);
    letter-spacing: 0.0125em;
    margin: 0;
    text-wrap: balance;
  }

  p + p {
    margin-block-start: calc(var(--step-1) * 0.675 * 1.55);
  }
  @media screen and (max-width: 1024px) {
    p {
      font-size: var(--step-0);
    }
  }
</style>
