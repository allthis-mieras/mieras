---
// TickerItem.astro
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class="marquee-item">{label}</span>

<style>
  span {
    font-family: var(--font-family-special);
    font-size: var(--step-3);
    padding: var(--space-l) var(--space-2xl);
    padding-top: calc(var(--space-l) + 0.2em);
    color: var(--background);
    background-color: var(--font-color);
    line-height: 100%;
  }

  /* span::after {
    content: "→";
    padding: 0 0 0 1vw;
  } */
</style>
